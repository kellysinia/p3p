@functions
{
    public static int intcon (string input)
    {
        if(input != "")
        {
            return Convert.ToInt32(input);
        }
        else
        {
            return -1;
        }
    }
}